# Terminal Collapse Generator Recovery v6 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` or `superpowers:executing-plans` task by task. Use `superpowers:test-driven-development` for every controller, parser, runner, and product-code change. Checkboxes are execution state; prose outside a checked step is never execution authority.

**Goal:** Preserve the failed v4 and v5 routes without promotion, publish the first executable Recovery v6 authority, spend one globally admitted generator/observer opportunity through a byte-bounded private-desktop runner, enforce the approved terminal-collapse rules, and close recovery at independently reviewed Task 2 completion.

**Architecture:** A small literal Windows PowerShell 5.1 bootstrap authenticates one phase-specific first leaf and the P6 plan buffer before extracting any executable block. The controller publishes M6 over exactly 127 predecessor artifacts, pauses for independent review, publishes L6 from the reviewed M6 hash, pauses again, and performs static Task 0. Task 1 compiles the P6-embedded `PrivateDesktopRunnerV6` through one bounded classic-csc row before its leaf-CreateNew admission, then produces fresh generator and observer evidence, a v6-only mother, and an exact 154-leaf completion. Task 2 consumes that completion, makes the exact three-file R6 change, runs nine evidence rows including the authenticated release-font gate, and publishes the exact 56-artifact Task 2 completion. Recovery v6 contains no Claude, Opus, copy, selection, replay, or Phase B body.

**Tech stack:** Windows PowerShell 5.1 Desktop; .NET Framework FileStream/SHA-256/ProcessStartInfo; fixed Git for Windows binary plus five DLLs; classic .NET Framework `csc.exe` under one explicit single-root trust contract; P6-embedded C# private desktop/Job/pipe runner; Ren'Py 8.5.2.26010301; bounded CPython 3.11 transient runtime; strict UTF-8 JSON.

## Global constraints

- The exact committed chain entering this plan is P2 `25c2ea674948ad89e8b48befb89643a8687648a4` → S3 `5fa8fb14792e095e066c3e9f698eda9ea4380854` → P3 `7365ae61c8d12dd0f34651a4bd727528cd9059d4` → S4 `f0ed56aaceb5cf8a0ff38fe2b888c13c3614ae98` → P4 `dccc785595f8551392c23db4a3a8d517fc6ef528` → S5 `2e1aa2b5c3a0618a8e4b4c6fcde4a1278437a651` → S6 `ecc82d888ac6523d1d16f7c20409113c3234cb19` → future P6 → future R6. There is no P5 commit or leaf. Every pre-R6 commit has game tree `fa7a398e9d989731b24e3c1642f3e2e33ce846ff`.
- S6 has subject `docs: specify terminal collapse generator recovery v6`, direct parent S5, and exactly one added path: `docs/superpowers/specs/2026-08-16-terminal-collapse-generator-recovery-v6-design.md`. Its physical and raw-blob bytes are identical: 52,875 bytes, SHA-256 `6C38BF4692DAF927B7A2BE50B215850931F1D883CC4B045B56B7E2412C394369`, blob `ac210ad08ca73f809fdc67b48bfa763308dafeca`.
- P6 must have subject `docs: plan terminal collapse generator recovery v6`, direct parent S6, and exactly one added path: this plan. Its physical bytes/SHA-256/raw blob and commit are discovered only after independent review and commit. This file must not self-embed those future values or any future M6/L6 physical hash.
- R6 is forbidden until Task 1 completion passes. It is P6's direct child with subject `fix: enforce terminal resistance collapse rules` and exactly three changed paths: `game/balance.rpy`, `game/difficulty.rpy`, and `game/test_game.rpy`. No merge, intermediate commit, hook side effect, extra path, staged residue, or parent drift is allowed.
- The fixed v6 runtime root is `.superpowers/sdd/terminal-collapse-ending/recovery-v6`. M6 is `recovery-v6/predecessor-evidence.json`; L6 is sibling `.superpowers/sdd/terminal-collapse-ending/approved-plan-lock-v6.json`. P4/M4/L4 and P5/M5/L5/recovery-v5 remain absent and are never adopted, repaired, deleted, renamed, or used as fallback.
- M6 schema 5 has exact ordered top-level fields `schema_version,purpose,last_sealed_plan_commit,last_sealed_spec_commit,last_sealed_lock_path,last_sealed_lock_bytes,last_sealed_lock_sha256,last_sealed_manifest_path,last_sealed_manifest_bytes,last_sealed_manifest_sha256,immediate_failed_spec_commit,missing_plan_path,missing_plan_present,artifact_count,catalog_bytes,catalog_sha256,artifacts,terminal_failure,controller_failure,preapproval_failure,source_inventories,created_utc`.
- M6 contains exactly 127 artifacts: the independently rebuilt S5 126-row union plus committed S5. Its canonical Ordinal catalog is exactly 27,437 bytes/SHA-256 `082B7B01E93437173FB97BD3764D14D8290ACE05F6E9CE48667F2D51921CA13F`. Review authenticates strict M6 metadata, schema, fixed known allowlist, and catalog metadata before opening any declared artifact path.
- M6 preserves the exact schema-28 v3 `terminal_failure`, schema-19 P4 `controller_failure`, and schema-22 v5 `preapproval_failure`. The v5 failure records seed invocation/exit `1/1`, A/B `0/0`, unknown stdout/stderr, no diagnostic artifact, no admitted assembly, and never-created M5/L5/recovery-v5. It never invents a compiler diagnostic or source line.
- The ignored `.superpowers/sdd/terminal-collapse-ending/p5-preapproval` package is nonauthority chronology only: four leaves/59,482 bytes, six directories, 390-byte normalized catalog/SHA-256 `CCDD3A17255ADA9B7A01EFFE43B21222D0078693D5841DD0940D3CDD0DDD51DC`. P6 executable bodies, M6/L6 publishers, reviewers, and tasks never open a package leaf.
- The three source inventories remain exact metadata: v2 generator worktree 8 authority/61 excluded; v2 SaveDir 12/0; v3 generator worktree 1/0 containing only the 8,845-byte patched fixture SHA-256 `A0D2A8B0589CC64F9479CE8E5B3315760001AA965502D1D740D35C6A2D558381`. Excluded leaves are never probed.
- M6 setup uses an ordinary, non-reparse, exactly empty root under a no-concurrent-writer premise. Only successful `FileStream(FileMode.CreateNew)` for M6 consumes publication. An interrupted pre-leaf setup may leave only the exact empty root and requires a separately authorized continuation; a nonempty or unproved root is terminal and is never deleted or adopted.
- L6 schema 6 has exact ordered fields `schema_version,purpose,approved_plan_path,approved_plan_commit,plan_bytes,plan_sha256,spec_path,spec_commit,spec_bytes,spec_sha256,immediate_predecessor_commit,missing_plan_path,missing_plan_present,last_sealed_plan_commit,last_sealed_spec_commit,last_sealed_lock_path,last_sealed_lock_bytes,last_sealed_lock_sha256,predecessor_manifest_path,predecessor_manifest_bytes,predecessor_manifest_sha256,predecessor_artifact_count,predecessor_catalog_bytes,predecessor_catalog_sha256,baseline_game_tree,execution_strategy,superseded_namespace,superseded_disposition,preserved_failure_report_path,preserved_failure_report_bytes,preserved_failure_report_sha256,task1_admission_ledger_path,task1_admission_limit,generator_attempt_ledger_path,generator_attempt_limit,observer_attempt_ledger_path,observer_attempt_limit,test_result_stream,engine_log_role,state_real_number_policy,execution_authorization_required,task1_admission_schema`.
- M6 publication receives the independently reviewed P6 physical SHA; P6 is its first and aliased authority/transport leaf. L6 publication receives reviewed M6 and P6 physical SHA values; M6 is first and P6 second. Every post-L6 Task 0/1/2 scope receives reviewed L6 and P6 SHA values; L6 is first and P6 second. The exact leaf is opened once under a retained deny-write/delete handle, read into a bounded buffer, hashed, parsed/extracted from that buffer, and never hash-then-reopened.
- Every controller target uses absolute `C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe`, Desktop 5.1, exact argv `-NoLogo -NoProfile -NonInteractive -EncodedCommand <bootstrap_b64>`, strict UTF-16LE/bootstrap base64, and the complete Windows quote round trip. The full command including terminal NUL must be at most 32,767 UTF-16 code units; the boundary tests accept 32,767 and reject 32,768.
- The small bootstrap reads one bounded canonical ASCII data frame, performs the phase-specific first/P6 leaf gates, extracts uniquely marked shared/phase blocks only from the authenticated P6 buffer, checks exact bytes/SHA/UTF-8/signature, and invokes them only via in-memory `ScriptBlock.Create`. Frame data is never code. `-Command`, `-File`, stdin script text, here-strings, temp scripts, shell interpolation, alternate encoding, fallback transport, and retry transport are forbidden.
- Hashing uses only `System.Security.Cryptography.SHA256` over exact ordinary non-reparse FileStreams. `Get-FileHash` is forbidden. The initial bootstrap uses only PowerShell language and .NET; module autoload is `None`, inherited `PSModulePath` is cleared, and any later Utility use imports and authenticates only `$PSHOME\Modules\Microsoft.PowerShell.Utility\Microsoft.PowerShell.Utility.psd1`.
- The fixed Git executable is `C:\Users\22325\.cache\codex-runtimes\codex-primary-runtime\dependencies\native\git\mingw64\bin\git.exe`, 4,344,704 bytes/SHA-256 `C115A66A1BEDE6694B513AF420CC90F8775BE03666A54D1ECB82D6196B929FE9`, version `git version 2.53.0.windows.3`. Its exact non-system application-directory DLLs are `libiconv-2.dll` 1,136,529/`FF31FA811F9C07CC7FDAA68C9E8BCA3A7B4FDF6E0A079A58175EA58BA139C7AE`, `libintl-8.dll` 298,731/`0537C3DD2378218508EBE3CC416D72A99EE2D24AE1C5525E23458F32544EF861`, `libpcre2-8-0.dll` 717,955/`C135A87ED0F11EAE8FFC4CB469671FF0B3F5D71FAB5FB024E9B1E7241CA25B52`, `libwinpthread-1.dll` 64,931/`D66B00E4A4385344BF2BE54B03446EA19CED654C78A18A024A0B43971D68459B`, and `zlib1.dll` 120,814/`CB7AB3788D10940DF874ACD97B1821BBB5EE4A91F3EEC11982BB5BF7A3C96443`.
- All Git calls use a clean fixed environment, absolute executable, exact argv row, concurrent capped byte drains, finite deadline, allowed exit before parse, empty-or-row-approved stderr, and pre/post executable/input seals. Path-list/record stdout uses supported `-z`/NUL framing; single root/OID has its own strict terminator grammar. Direct `& git`, `$LASTEXITCODE`, text path parsing, PATH Git, `Start-Process`, ignored exit, parse-before-exit, and fallback are Critical.
- `Invoke-V6Process` uses one immutable operation row, `UseShellExecute=false`, `CreateNoWindow=true`, empty exact environment, explicit executable/cwd/argv, concurrent fixed-size BaseStream drains, byte caps, one deadline, and a bounded cleanup allowance. It makes no Job, loaded-module, private-desktop, or descendant-containment claim.
- The sole classic-csc row uses `C:\Windows\Microsoft.NET\Framework64\v4.0.30319\csc.exe`, 2,569,832 bytes/SHA-256 `46809206887326D2D24DB1EFF1F3064DE972C3451ABE766B49111450A5E08E00`. P6 freezes its ordinary input files, exact three framework references, clean environment, supported argv, caps/deadline, and exact scratch/output set. It omits `/deterministic`, `/pathmap`, response/config defaults, and debug output. This exact row is an explicit single-root trust assumption: it does not claim Job/debug/module/short-lived-descendant proof and accepts only root exit, EOF, allowed exit, scratch/post-seals, and no surviving related process.
- P6 contains one marked strict-UTF-8 `PrivateDesktopRunnerV6` C# block and one PowerShell adapter block. It writes/compiles/loads them only after P6 authority, loads DLL bytes with `Assembly.Load(byte[])`, and never uses `Add-Type` or the historical helper pair. Compile roots are exactly `C:\Users\22325\AppData\Local\Temp\CourtOfShadows-V6-PrivateDesktop-Review`, `C:\Users\22325\AppData\Local\Temp\CourtOfShadows-V6-PrivateDesktop-Task1`, and `C:\Users\22325\AppData\Local\Temp\CourtOfShadows-V6-PrivateDesktop-Task2`; phase-mismatched roots are forbidden and all three are absent before P6 approval.
- `PrivateDesktopRunnerV6` creates a fresh private desktop and Job; admits a suspended target atomically; supplies only the exact stdin/stdout/stderr handles; starts finite cap-enforcing readers and window monitor before the sole resume; and returns bounded in-memory streams plus an exact result. PASS requires root exit, clean EOF, no cap/fault, Job ActiveProcesses zero, exact accounting, zero visible windows, monitor completion, and cleanup. It never gives target output a disk file and never uses PATH, `-File`, a second PowerShell host, inherited semantic environment, `ReadToEnd`, or `ReadAllText`.
- The historical `PrivateDesktopRunner.cs` and `Invoke-PrivateDesktopProcess.ps1` remain M6 artifacts but are never dot-sourced, invoked, copied, compiled, or used as behavior authority in v6.
- Task 0 runs exactly the P6-frozen count/order of read-only Git rows and no other Git operation. It launches zero helper/Ren'Py/Python/scanner/Claude/Opus processes, creates zero durable rows, and consumes no Task 1 opportunity.
- Task 1 first compiles/loads the v6 runner and proves compile scratch absent, then creates an ordinary exactly empty `task1-admission` setup directory and atomically CreateNews `attempt.json`. Only successful leaf CreateNew consumes the one global Task 1 opportunity. Any later failure is terminal; there is no retry, new GUID, alternate worktree/SaveDir/evidence root, delete/recreate, or transport change.
- RED/GREEN are schema 4 exact 8. JSON-real has exactly eight positive and eighteen negative cases and accepts only the exact per-case native Decimal or finite Double contract. The 42 named mutations remain exact/ordered and do not include JSON-real cases.
- Generator attempt/completion schemas remain 3 exact 19/43; observer 3 exact 20/42. Each binds admission and permits exactly one invocation. Before Task 1 completion the v6 tree has exactly 24 leaves/six directories; after completion 25/six. The exact Task 1 union is 127+27=154. Completion is schema 6 exact 16 and `lineage_status="fresh_v6_only"`.
- Task 2 begins in a fresh L6→P6 scope, validates Task 1 and all 154 seals, applies exact R6, and runs exactly nine invocations in the P6-frozen order. Each receipt is schema 1 exact 14; union is exact 56; Task 2 completion is schema 6 exact 16. The release-font gate authenticates the 2,908-file/218-directory transient runtime and 58-path/1,240-byte scan catalog SHA-256 `AA5433C2D931CEE1052244AFCE0CAFC0C773FBA5F14285871B9B6841DF1DD7A3`, proves `game/msyh.ttf` byte-identical, and leaves zero runtime/cache/backup residue.
- Recovery v6 ends immediately after independent Task 2 completion review. No v6 body or vector contains Task 3, Claude, Opus, setup-token, credential, copy, candidate, blind-map, selection, replay, or Phase B logic. Post-R6 copy requires a new spec/plan/lock, three fresh Opus A/B/C runs, raw presentation to the user, user selection, and then a separate post-selection replay lineage.
- The protected untracked winter plan is exactly `docs/superpowers/plans/2026-08-09-winter-interlude-narrative-delivery.md`, 549,769 bytes/SHA-256 `0F39B5F5ACE1D4666DD146863CABDF398B031F5666C29AE337CEB89796E4276C`. It is never edited, staged, chmodded, moved, deleted, or included in a recovery commit.
- S6/P6/M6/L6/Task0/Task1 add no game asset. R6 adds zero asset/font payload and changes only scripts; actual compiled-script/archive/package delta is measured from the release build rather than claimed zero. Art, music, sound effects, animation, UI, and new font assets are not required for Recovery v6.

## File and interface map

- Existing tracked design: `docs/superpowers/specs/2026-08-16-terminal-collapse-generator-recovery-v6-design.md` — committed S6, immutable.
- Create tracked plan: `docs/superpowers/plans/2026-08-16-terminal-collapse-generator-recovery-v6.md` — P6, sole-path child of S6 and the only executable code/body authority for recovery v6.
- Create ignored authority: `.superpowers/sdd/terminal-collapse-ending/recovery-v6/predecessor-evidence.json` — M6 schema 5 exact 22/127.
- Create ignored lock: `.superpowers/sdd/terminal-collapse-ending/approved-plan-lock-v6.json` — L6 schema 6 exact 42.
- Create ignored Task 1 authority/evidence: `recovery-v6/task1-admission/attempt.json`, RED/GREEN, generator/observer attempt/process/state/fixture/log/completion leaves, `mother`, baseline, and `task1-completion.json` — exact 24→25/six and 154 union.
- Create fixed Task 1 temporary roots: `E:\Projects\renpy-8.5.2-sdk\terminal-collapse-temp\cos-terminal-collapse-generator-recovery-v6`, `E:\Projects\renpy-8.5.2-sdk\terminal-collapse-temp\cos-terminal-collapse-generator-save-recovery-v6`, `E:\Projects\renpy-8.5.2-sdk\terminal-collapse-temp\cos-terminal-collapse-observer-recovery-v6`, and `E:\Projects\renpy-8.5.2-sdk\terminal-collapse-temp\cos-terminal-collapse-observer-save-recovery-v6` — exactly two worktrees and two SaveDirs, removable only by successful controlled cleanup.
- Modify tracked only in Task 2: `game/balance.rpy`, `game/difficulty.rpy`, `game/test_game.rpy` — R6 exact three paths.
- Create ignored Task 2 authority/evidence: `recovery-v6/rules/` and `task2-completion.json` — nine receipts/56 union/schema 6 exact 16.
- Create/delete nonproject only: the three fixed v6 compiler roots and the exact release-font transient runtime. They never become authority artifacts.

## Execution and review gates

1. Commit/review P6 alone at C0/I0; only nonproject controller and synthetic runner controls may run before approval.
2. Separately authorize/publish M6, stop, and obtain an independent uppercase physical M6 SHA.
3. In a fresh M6→P6 scope, separately authorize/publish L6, stop, and obtain an independent uppercase physical L6 SHA.
4. In a fresh L6→P6 scope, separately authorize Task 0 and require its exact static PASS.
5. In a fresh L6→P6 scope, separately authorize Task 1 once; compile the runner before admission, then consume admission and complete Task 1 without retry.
6. Independently review Task 1 completion and all 154 leaves.
7. In a fresh L6→P6 scope, separately authorize Task 2/R6 once; complete nine rows and publish Task 2 completion.
8. Independently review Task 2 completion. Stop Recovery v6. Do not continue into copy or winter work from this plan.

---
